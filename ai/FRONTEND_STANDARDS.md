# FRONTEND_STANDARDS

Frontend code must be maintainable and predictable.

## Rules
- separate view, state, and API interaction concerns
- avoid business logic in components where possible
- centralise state transitions
- prefer typed interfaces and explicit props/contracts
- test critical UI logic and form validation
- isolate external API calls behind client modules
